<%= render partial: 'partials/system/form_errors', locals: { object: @house } %>

<%= form_with model: [:system, @house] do |form| %>
  <%= form.label :name %><br>
  <%= form.text_field :name %><br>

  <%= form.submit %>
<% end %>
